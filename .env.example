# Copy to `.env` and fill in real values. `.env` is git-ignored; this example
# file is committed as a reference. Nothing loads `.env` automatically yet — add
# a loader (e.g. python-dotenv) if your project needs one.
#
# Note: GIT_USER_NAME / GIT_USER_EMAIL are read from your *host* environment by
# the dev container (see README), not from this file.

MY_ENV_VAR=
